version: 2

sources:
  - name: bronze
    schema: bronze
    tables:
      - name: netflix_titles

models:
  - name: stg_netflix_titles
    columns:
      - name: show_id
        tests:
          - unique
          - not_null
      - name: type
        tests:
          - not_null
          - accepted_values:
              values: ['Movie','TV Show']
      - name: title 
        tests:
          - not_null